var spellchecker = function(wordlist, queries) {
    let caps = {}
    let vowels = {};
    for (let i = 0; i < wordlist.length; i++) {
        let word = wordlist[i];
        caps[word.toLowerCase()] = word;
        
    }  

};