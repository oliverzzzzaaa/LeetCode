var loudAndRich = function(richer, quiet) {
    let lookup = new Array(quiet.length);
    lookup.fill(0)
    function dfs(idx, curr) {
        for (let i = 0; i < richer.length; i++) {
            if ()
        }
    }   
    for (let i = 0; i < lookup.length; i++) {
        dfs(i,0)
    }
};